const authResponse = require('./auth');
const exceptionResponse = require('./exception');
const listResponse = require('./list');
const dataResponse = require('./data');
const commandResponse = require('./command');

module.exports = {
    authResponse,
    exceptionResponse,
    listResponse,
    dataResponse,
    commandResponse
}